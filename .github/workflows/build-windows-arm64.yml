name: Build Windows ARM64

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    name: Build Windows ARM64
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Get version
        id: version
        run: echo "VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo 'dev')" >> $GITHUB_OUTPUT

      - name: Tidy main module
        run: go mod tidy

      - name: Build main binary
        env:
          GOOS: windows
          GOARCH: arm64
          CGO_ENABLED: 0
        run: |
          mkdir -p bin
          go build \
            -ldflags "-s -w -X main.version=${{ steps.version.outputs.VERSION }}" \
            -o bin/forgejo-forge-windows-arm64.exe .

      - name: Tidy installer module
        working-directory: forgejo-installer
        run: go mod tidy

      - name: Build installer binary
        working-directory: forgejo-installer
        env:
          GOOS: windows
          GOARCH: arm64
          CGO_ENABLED: 0
        run: |
          mkdir -p ../bin
          go build \
            -ldflags "-s -w" \
            -o ../bin/forgejo-main-windows-arm64.exe .

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: forgejo-forge-windows-arm64
          path: |
            bin/forgejo-forge-windows-arm64.exe
            bin/forgejo-main-windows-arm64.exe
          if-no-files-found: error
          retention-days: 7

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            bin/forgejo-forge-windows-arm64.exe
            bin/forgejo-main-windows-arm64.exe
          generate_release_notes: false
